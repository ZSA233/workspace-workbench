# Workspace Workbench v0.3.1

This patch makes the Gitlink fixture tests independent of the runner's global Git identity.

- Temporary Gitlink child repositories now receive an explicit fixture author identity before tests create child commits.
- The release contents and shared HTTP MCP gateway are unchanged from v0.3.0.

Validation: TypeScript typecheck, the full 223-test suite, and the release metadata check passed locally with the Git global configuration disabled for Gitlink tests.
