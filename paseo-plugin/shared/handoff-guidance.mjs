export const coordinatorGuidance = "For a simple Git worktree use workbench_workspace_create first; it does not need an Agent host connection. If an MCP call fails, check workbench_connection_status and retry after recovery. If a write may have been dispatched, query its status using the same requestId; never create manual worktrees or replay it with a new identity. If a worker is requested, call workbench_workspace_delegate separately with the handoff. The optional workbench_workspace_preview freezes materials locally and does not create Git state; workbench_workspace_submit remains the one-step handoff path. Do not mechanically duplicate conversation history, MIME metadata, decisions, or rejected alternatives. After a successful handoff, report it and end this turn. Send later supplements through workbench_session_message.";
export const handoffCompleteGuidance = "执行会话已接手。请告知交接结果并结束本轮；后续补充使用 workbench_session_message，不要自行实施或重复执行计划。";
export function handoffOutcome(result) {
  return result?.ok ? { ...result, nextAction: "end_turn", instructions: handoffCompleteGuidance }
    : { ...result, nextAction: "check_status", instructions: "Check the existing handoff status; do not recreate or implement the task when delivery is uncertain." };
}
